
print() และ echo() ต่างกันยังไง
1.print มีการส่งค่ากลับมา (return) แต่ echo จะไม่มีการส่งค่ากลับไป (void)
2.print สามารถระบุค่าได้ตัวเดียว แต่ echo มีได้หลายตัว
3.echo เร็วกว่า print

ตัวแปร (Variables) ใช้สำรหับเก็บข้อมูลเพื่อใช้ประมวลผล เช่น ตัวเลข ตัวอักษร กลุ่มข้อมูล ข้อความ
โครงสร้างของตัวแปร $ชื่อตัวแปร = ค่าของตัวแปร
กฏการตั้งชื่อตัวแปร
1.สามารเป็นได้ทั้งตัวใหญ่ตัวเล็ก ตัวเลข สัญลักษณ์ _(underscores) และ $(doller sign)
2.อักษรตัวแรกห้ามเป็นตัวเลข
3.ตัวแปร ตัวเล็กตัวใหญ่ มีความหมายต่างกัน
4.ไม่ซ้ำกับ buit-in function (ฟังก์ชั่นมาตรฐานใน PHP)
5.ตัวแปรขึ้นต้นด้วย $(doller sign)
6.ตัวแปรไม่สามารถเว้นวรรคได้ หรือเคาะบรรทัดได้

ชนิดข้อมูลของตัวแปร 
1.Integer ตัวเลขจำนวนเต็ม
2.Float/Double เลขจำนวนจริงหรือทศนิยม
3.Boolean ข้อมูลตรรกศาสตร์มีค่าเป็น True และ Flase
4.String ตัวอักษรหรือกลุ่มข้อความ
5.Array กลุ่มหรือชุดข้อมูลที่มีชนิดเดียวกัน
6.Object การกำหนดให้ตัวแปรนั้นเก็บคุณสมบัติของ Object (Attribute & Method) โดยประกาศใช้งานผ่าน Class (OOP)

ค่าคงที่ (Constant) การสร้างตัวแปรที่เก็บข้อมูลและไม่สามารถเปลี่ยนค่าข้างในได้ (ชื่อตัวแปร,ค่าที่กำหนด) โดยปกติจะใช้เป็นตัวพิมพ์ใหญ่ทั้งหมดเพื่อแยกประเภทของตัวแปรที่สร้างว่าอันไหนเป็นค่าควที่ หรือ ไม่ได้เป็นค่าคงที่

ชังก์ชั่นที่ทำงานเกี่ยวกับตัวแปร
1.isset คือฟังก์ชั่นสำหรับตรวจสอบส่าตัวแปรมีการกำหนดค่าหรือไม่ ถ้ามีการกำหนดค่า จะมีค่าเป็น จริง(1) ถ้าไม่ได้กำหนดจะมีค่าเป็น False 
2.unset คิอการยกเลิกตัวแปรและคืนค่าให้หน่วยความจำ
3.empty ฟังก์ชั่นสำหรับตรวจสอบว่าตัวแปรมีค่าว่างหรือเลขศูนย์หรือไม่ ถ้ามีการกำหนดค่า จะมีค่าเป็น จริง(1) ถ้าไม่ได้กำหนดจะมีค่าเป็น False (0)
4.is_null ฟังก์ชั่นสำหรับตรวจสอบว่าตัวแปรมีค่าว่างหรือไม่
5.print_r() ฟังก์ชั่นสำหรับแสดงค่าตัวแปร array
6.var_dump() แสดงรายละเอียดตัวแปร

ตัวแปร Superglobal
1.$GLOBALS เป็นการประกาศให้ตัวแปรเป็น global เพื่อให้ทุกส่วนสามารถเรียกใช้งานได้
2.$_SERVER เก็บค่าต่างๆของ web server ที่กำลังทำงานอยู่
3.$_GET เป็นตัวแปรแบบ Array ใช้เก็บค่าที่ส่งมากับ URL
4.$_POST ใช้เก็บค่าที่ส่งมากับ From แบบ post Method
5.$ENV ตัวแปรที่จัดเก็บสภาพแวดล้อมทั่วไปและค่าต่างๆของ SERVER
6.$_SESSION เก็บตัวแปร session
7.$_COOLIE เก็บตัวแปร cookie

ตัวดำเนินการ (Operator) กลุ่มของเครื่องหมายหรือสัญลักษณ์ที่ใช้ในการเขียนโปรแกรม A+Boolean 1.ตัวดำนเนินการ (Operator) 2.ตัวถูกดำเนินการ (Operand)
ตัวดำเนินการเปรีบเทียบ
 == เท่ากับ
 ==== เหมือนกัน
 != ไม่เท่ากับ
 <> ไม่เท่ากับ
 !=== ไม่เหมือนกัน
 > มากกว่า
 < น้อยกว่า
 >= มากกว่าเท่ากับ
 <= น้อยกว่าเท่ากับ
 <=> spaceship (php7)